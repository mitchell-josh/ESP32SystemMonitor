#ifndef LV_CONF_H
#define LV_CONF_H

#include <stdint.h>

// This tells LVGL to just use the default settings since we 
// are defining the important stuff in platformio.ini
#define LV_CONF_SKIP 

#endif